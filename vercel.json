{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "[ -f vite.config.ts ] && pnpm exec vite build || (pnpm --filter @paperclipai/ui exec vite build && mkdir -p dist && cp -r ui/dist/* dist/)",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
